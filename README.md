
# Minimal Static Site (GitHub → Vercel)

**How to edit**
- Open `index.html` in GitHub (press `.` to open the web editor), change text, and commit.
- Vercel will auto‑build and redeploy in seconds.

**Local preview (optional)**
- You can just open `index.html` in your browser without any build step.
